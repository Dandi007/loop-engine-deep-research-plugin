{"command_id":"cmd_01KZ6XC2HBYS15E1YJG20MMQFK","contract_version":"dev-dispatch.attempt-context/v1","decided_at":"2026-08-04T17:33:08Z","decision":"APPROVE","development_id":"dev_ledr_a7_plugin_wiring_01","evidence_bundle_digest":"sha256:df75c33777b10e458ac5bdaa02da327f732552d20ec74cdcc4ec36685ab9c56b","expected_handoff_commit":"33bd9db816f353e22ca9d26af481bd0d301d2a56","expected_handoff_receipt_digest":"sha256:a281b616dad81fb8f2c1e2204957ae4e0e62cdec93990a7eddeb4fb92fbee298","expected_target_head_commit":"a4b14d01b7dee4437f927ee14021d29479765edc","gate_id":"gate_01KZ6X8FJZX802FZC3X8GR88EW","kind":"gate_decision","operator_identity":"line-deep-research","reason":"APPROVE —— attempt 1 一次过。全部 gate 义务由本 gate 亲自执行。\n\n【1. 变异功率（亲跑，回显被改的行，跑完逐字还原）】\n  U1 `config_dir` 指向不存在的路径 ⇒ 杀 **G3**（+ G4）\n  U2 `seed[].template` 指向不存在的名字 ⇒ 杀 **G4**\n  U4' 同时改坏 `exports[\".\"]` 与 `main` ⇒ 杀 **G8**\n\n  ⚠️ **U4 首轮零功率是我的变异构造错误**：G8 的断言是\n  `pkg.exports?.[\".\"] ?? pkg.main`，我只改了 `main`（**fallback 分支**），\n  `exports[\".\"]` 仍有效 ⇒ 断言取到有效值、照常通过。\n  > **判据：断言里有 fallback 链（`a ?? b`）时，只变异 `b` 什么也证明不了 ——\n  > 必须变异 `a`，或两个都变。**\n  改正为同时变异两者后精确杀死 G8。\n\n【2. G5 的实质判定（我的 grep 写弱了，故亲自核语义）】\n  渲染后的 fleet **确实含 `claim.store_dir`**，指向\n  `.runtime/deep-research/<run>/stores/trigger`。\n  我的 G5 grep 找的是 `clue.*store_dir`，该 store 名为 `trigger` ⇒ **字面通过**。\n\n  **亲核语义后判定 G5 的意图成立**：\n  · `trigger` store 是 loop-engine 的**驱动机制**（认领一个 trigger → 跑 tick → 完成），\n    属 `golden-order` 允许的「FS 作临时/工作面」；\n  · tick 节点模板调的是 `tick_entry --selfcheck`，**不 import `./bus`**（G7 已断言）；\n  · **没有任何 store 承载 clue 状态** ⇒ 「不存在第二份需要同步的真相」未被破坏。\n\n  > **但记录在案：G5 的检查方式（按 store 名 grep）不能测量它想测的属性。**\n  > 正确写法应是「渲染后所有 `claim`/`pending` 的 store 中不得出现 clue 语义字段」，\n  > 或直接断言 tick 节点不写任何 store。**这是本 gate 的判据缺陷，不是交付缺陷。**\n\n【3. 机械核验】\n  身份三维全对；spec 漂移 0；**测试声明新增 28、删除 0**。\n  acceptance 三条 exit 0，**141/141**，`development_id` 为本包。\n  我跑变异的树与被验收的 subject 之间，`src/tick-entry.ts` / `test/plugin-wiring.test.ts` /\n  `bin/deep-research-loop.sh` / `workflows/deep-research/fleet.yaml.tpl` / `package.json`\n  blob oid **逐字相同**。\n  G1（`--dry-run` exit 0）我在干净 `npm ci` 后**亲自执行**：exit 0、渲染出合法 fleet。\n  G10（`.dd-evidence/` diff 为空）成立；G11（`docs/dev-notes/<dev_id>.md`）成立。\n  G9：`src/tick-entry.ts` import 自 `./tick`，**未新增第二份决策实现**。\n\n────────────────────────────────────────\n【⛔ 必须说准的范围：本包关掉的是「不可加载」，**没有**关掉「不可运行研究」】\n\ntick 节点的模板硬接的是 `tick_entry --selfcheck` —— 在**空板面**上跑纯决策、不连 bus。\n**这完全符合本 spec §3 与 §7 的禁令**（不连真实 bus / 不做真实端到端，属 V1），\n**实现方做的正是我要求的**。\n\n⇒ 合入后得到的是：**可被 loop-engine 加载的 plugin 骨架 + 一个无副作用自检节点**。\n⇒ **不是**可运行的研究循环。\n\n> **这是「装配缺口」判据往下一层的复现**：\n> 我在 A7 里补上了「装配」，却又在 A7 内部把装配切成了**结构装配**而没有**行为装配**。\n> **缺口从「六个能力包之间」挪到了「A7 与 V1 之间」。**\n>\n> ⇒ **判据升级：「可运行」不是一个可以留到最后一个包去满足的属性。\n> 每次为了隔离风险而把「连真实依赖」推后时，都要同时立项那个「接上去」的包，\n> 否则它会再一次落进缝隙。**\n\n【已立项的后续包（本 gate 记录，不在本包范围）】\n  **A8 —— tick 接真实 bus**：给 `tick-entry` 增加 `--run` 模式（import `./bus`，\n  按配置的 channel 跑真实 tick），并把节点模板从 `--selfcheck` 切到 `--run`。\n  ⚠️ **A8 依赖链 C 的 role 存在**——否则 tick 派 worker 时无可派目标。\n  ⇒ 顺序为：**链 C（A1→R1→R2）→ A8 → V1**；链 B 的 R4 可与之并行\n    （R4 的「SKILL.md 指向新入口」现已可指 `bin/deep-research-loop.sh`）。\n\n【明确未被证明的部分 —— 不随本次放行一并声称】\n  · **plugin 从未被 loop-engine 真实加载过**：G1–G4 证明的是「渲染结果自洽、引用的文件都存在」，\n    **不是**「loop-engine 接受它」。spec §8 明确 `--dry-run` 不得依赖 CLI 真实可执行。\n  · 本包**未连真实 bus / MinerU / vault**，三者的可达性与写入权限均未验证，属 V1。\n  · 变异只覆盖三处结构引用；G6/G7/G12/G13 等未被任何变异针对，**其功率不由本包证明**。\n"}
