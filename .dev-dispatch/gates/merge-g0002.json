{"command_id":"cmd_01KZ73K0NV6ZATFY95QPKB78SZ","contract_version":"dev-dispatch.attempt-context/v1","decided_at":"2026-08-04T19:21:50Z","decision":"APPROVE","development_id":"dev_ledr_a8b_tick_write_01","evidence_bundle_digest":"sha256:44dd9dd739f76e4a2fe4c73860099f38947ce78d41f279bce9ebc06562d442b0","expected_handoff_commit":"c84a7f01e7b244e9dee28f6a88b9d3d43ada1c20","expected_handoff_receipt_digest":"sha256:755acf6168b6d714861590bf8d966ae816cc06646d817da2dda98815220a4d5b","expected_target_head_commit":"c0d084a582b4546f3ff71d52064166bf676a3da4","gate_id":"gate_01KZ73CC8E2TF9G1V16NG77HFR","kind":"gate_decision","operator_identity":"line-deep-research","reason":"APPROVE —— attempt 2。全部 gate 义务由本 gate 亲自执行。\n\n【1. attempt 1 的 blocker 是真的，且比我发现的严重得多 —— 我完全漏了】\n\nreviewer 抓到 `src/tick-run.ts` 的 `realCas` **破坏 CAS 互斥的同源读**：\n决策的前置条件来自**板面快照**，而 `supersedes` 取自**第二次独立的 `getEntity` 读**，\n且无 head status 守卫 ⇒ 若另一 worker 在两次读之间抢到该卡，\n`getEntity` 返回它的 `in_flight` head，`casUpdateClue` **成功 supersede 它**并写入自己的 run_id\n——**静默覆盖一个活认领，不报 409**。\n\n**这正是 2026-08-03 那次真实事故的形态**（两条线各自认为持有同一槽、无人拿到 409）。\n\n> ### ⛔ 判据：一条不变量在某一层被守住，不构成它在别的层也被守住。\n> **S1b 用整整一个包**钉死了 `claimClue` 的同源读（含 M4 变异守卫）；\n> **`realCas` 绕过 `claimClue` 直接调 `casUpdateClue`，就把同一个缺陷重新引入了。**\n\n> ### 🔑 我为什么没抓到 —— 比缺陷本身更值得记\n> 我带着「**runs 接线有缺口**」这个**已知假设**去 review，\n> 验证完 `tick-run.ts:242` 附近就停了，**从没读 `realCas`（214-228）**。\n> **带着一个已知假设去 review，会让你只看到那个假设覆盖的区域。**\n> 与本 session 已记的两次「拿更熟悉的解释去 confirm 而不证伪」同族，\n> 只是这次不是解释错，而是**搜索范围被假设裁剪了**。\n> ⇒ **纪律：预判到的缺口验证完之后，必须再做一遍不带假设的通读。**\n\n【2. attempt 2 的修复：我亲跑变异确认真有功率】\n\n  **Z1**（去掉同源 status 守卫，重现 attempt 1 的缺陷）⇒ 杀 **2 条**：\n    · `dispatch when head already in_flight (another worker) ⇒ conflict, no publish`\n    · `reclaim when head no longer in_flight ⇒ conflict, no publish`\n  **X3**（去掉 `run_id` 写入）⇒ 杀 **M7**：\n    · `realCas publish body carries a non-empty run_id in payload (X3 kills M7)`\n\n  修复形态正确：`current` 与 `supersedes` **同取自一个 `head`**，\n  并加 `current !== input.from ⇒ conflict` 守卫 —— 与 S1b `claimClue` 同形。\n  `realCas` 已**导出**，可被直测（attempt 1 时它未导出、无测试，是 X3 杀 0 条的根因）。\n\n【3. reviewer 第二条 blocker 同时指出我的变异表有一行是错的】\n  M7 原先打在**注入的 `WriteDeps.cas` 入参**上，而非 `realCas` 的真实 publish body\n  ⇒ 我的 **X3→M7 归因本身不成立**（X3 打在那行不会杀 M7）。**reviewer 比我先发现。**\n  attempt 2 已改为断言真实 publish body。\n\n【4. 机械核验】\n  · acceptance `passed: True`，`development_id` = 本包，三条命令 exit 0，**171/171**\n  · 树身份：`tick-run.ts` / `tick-inspect.ts` / `tick-run.test.ts`\n    与 subject `776a0c82` blob oid **逐字相同**\n  · **M14**：actor 提交 `03bb11b` **未碰 `.dd-evidence/`**\n  · M1/M2 是**正确的判别性对**（只差 `runs` 一项、断言相反）\n  · M9：spawn dep 调用 0 次且 `pendingSpawns` 有记录（**安全性+活性配对**）\n\n────────────────────────────────────────\n【⛔ 仍未被守住的一处 —— 折进 A8c，本 gate 明确不声称已解决】\n\n**「读 runs」与「用 runs」之间的接线无守卫。** 我实测：\n```\nX1' readAgentRuns 永远返空            ⇒ 杀 M6 ✓\nX1  调用点 const runs = await readAgentRuns(...) → {}   ⇒ 杀 0 条 ✗\n```\nM1/M2 证明 `decideTick` 会依 runs 判别（那是 **S2** 的逻辑，纯数据）；\nM6 证明 `readAgentRuns` 真的读。**两者之间没有任何断言。**\n把 `assembleBoard(messages, runs)` 的 runs 换成 `{}`，**M1/M2/M6 全部照过**。\n\n> **判据：把一个能力拆成「取数」与「用数」两半分别验证，\n> 不构成验证了它们相连。必须有一条端到端的判别性用例。**\n\n⇒ **折进 A8c 硬验收**（A8c 必然重写 `runTick` 以加 spawn）：\n  「同一板面，bus 上**有** `agent.run.started` ⇒ 不回收；**无** ⇒ 回收」的**集成级**判别用例。\n\n⛔ **在 A8c 落地前，`--run` 不得对有在飞卡的板面使用。**\n\n【另记一处 check 设计的教训】\n  `tick-entry.ts` 的注释直白写着「用**无字面量**的空 runs（M6 要求生产路径不得硬编码\n  空的 runs 字面量）」—— `Object.create(null)` 是**为绕过我的 grep 选的写法**。\n  该位置语义无问题（空板面自检，`cards: []`），但它证明：\n  **对字面量做 grep，永远可以换个写法绕过。**\n\n【明确未被证明的部分 —— 不随本次放行一并声称】\n  · **`--run` 从未对真实 bus 执行过写入**（M13 的真机验证在本包由实现方跑，\n    我**未独立复现**——因为写入不可回退，且接线缺口未修前不宜再写）。\n  · spawn **完全未实现**（属 A8c）。\n  · 变异只覆盖 Z1/X1/X1'/X3；M8/M10/M11/M12 的功率**不由本 gate 证明**。\n"}
