{"command_id":"cmd_01KZ7SAD9PW83KYD2BRYR143CF","contract_version":"dev-dispatch.attempt-context/v1","decided_at":"2026-08-05T01:41:35Z","decision":"APPROVE","development_id":"dev_ledr_a8f_adddir_revision_01","evidence_bundle_digest":"sha256:f7c81c4b31372b282b06c2ad1ec6e2aef471b959c834648445c1a9fde7825187","expected_handoff_commit":"9c7b4077a991895dd5ce1abb44a05d05f58b32b3","expected_handoff_receipt_digest":"sha256:3978aa029a3c0a732e02cb084cb40b3674d88a4041ab099f994955e6bf1bd4cc","expected_target_head_commit":"12261d8b67e5b1293357bacffc5f80052ca6feee","gate_id":"gate_01KZ7S2B78QMVSSX44FBS9GNH6","kind":"gate_decision","operator_identity":"line-deep-research","reason":"APPROVE —— attempt 2。**V1 前最后一个包。**\n\n【0. 本包关掉的缺口：引擎派出的 worker 什么都读不到，且不报错】\n\ngrep 级实测三条：`role.fs` 全仓无实际消费者（`dispatch.ts` 从不读它）、\n生产 argv 不含 `--add-dir`、`allowed_root` 从没被填过\n⇒ code-local worker 在空工作区外**读不到任何东西 ⇒ 零证据、不报错、不崩溃**。\n\n⚠️ **这个缺口是本 gate 自己的手工测试掩盖掉的**：三次真跑有证据，\n只因命令行手工加了 `--add-dir` —— 生产路径不传它。\n> **判据：手工跑若加了生产不传的参数，它就不是对生产路径的测试。**\n\n【1. 我独立复跑】\n  `npm ci` → `tsc --noEmit` exit 0 → `vitest run` = **254 pass / 0 fail**（A8e 后 238，净 +16）。\n\n【2. 变异功率（亲跑，逐条回显被改的那一行，每次 `git diff --stat` 验证还原）】\n\n  **G5**（删掉 `throw new MissingAllowedRootError(role)`）⇒ 精确杀 **F5**\n    （`code-local without allowed_root ⇒ loud failure naming allowed-root, zero spawn`）\n    —— **这是本包存在的理由**：静默零证据是最难诊断的失败。\n  **G7**（断掉链路最后一层：`tick.md` 不传 `--allowed-root`）⇒ 精确杀 **F1**\n    （`deep-research-loop.sh → fleet → workflow → tick.md → --allowed-root`）\n    —— **接线判据第一次被主动验证有牙**。\n  **G4**（git 失败时返回 `\"\"` 而非 `undefined`）⇒ 精确杀 **F9**\n    （`resolveRevision on a non-git dir returns undefined, not ''`）\n    —— 与 A8e 的 `\"://@\"`、与「解析不到 secret 不得塞空串」同族。\n\n  G1/G2/G3/G6 未由本 gate 构造。\n\n【3. ⚠️ 我实测到一次 `npm test` 偶发失败，根因已定位，方向已判定】\n\n首次全量跑：**F1 挂**（`expected null to be '/data/code/self/agent-runtime'`）。\n随后单文件连跑 2 次全过、全量连跑 3 次全过、手工 render 完全正确\n（`allowed_root: /data/code/self/agent-runtime`）。\n\n**根因（实测，非推断）**：\n`bin/deep-research-loop.sh:19` `RUN_ID=\"${DD_RUN_ID:-$(date +%Y%m%d-%H%M%S)}\"`\n—— **秒级时间戳**；而 **4 个测试文件**（`a8f-adddir` / `harvest` / `plugin-wiring` / `tick-run`）\n都调该脚本 render。vitest 默认并行 ⇒ 同一秒内共用同一个 `RUN_ROOT/fleet.yaml`，互相覆盖。\n\n**偶发方向已判定为「只会假失败、不会假通过」**：另外 3 个文件**从不设 `ALLOWED_ROOT`**\n（各自 grep 命中 0 次）⇒ F1 若读到它们的产物只会看到 `null` 而挂；\n**没有任何测试会写出那个期望值** ⇒ 不存在假通过路径。\n\n⇒ **不阻断放行**（假失败不掩盖回归），但**记为必须修的遗留**：\n⛔ **本仓下一个包必须让 render 产物路径按测试隔离**（如 `DD_RUN_ID` 注入唯一值）。\n**理由：`npm test` 是本仓所有后续包的 acceptance 闸门，一个会假失败的闸门会侵蚀对它的信任。**\n\n⚠️ 且这与 attempt 1 被拒的 finding **同族**（夹具 `gitDir:true` 静默覆盖调用方的 `allowedRoot`，\n使 F4 拿 A 仓的 sha 比对 B 仓的 revision）——**验证装置自己失效**，本轮第五次。\n\n【4. 机械核验】\n  · acceptance `passed: True`；`typecheck` / `test` 均 exit 0\n  · **F14**：actor 提交未触碰 `.dd-evidence/`\n  · **F16**：既有用例**删除 0**，238 → 254\n  · **§4 要求已兑现**：dev-notes 46-48 行明写\n    「⛔ 本包不用 `--add-dir` 做安全隔离……唯一作用是『让 worker 读得到』，\n     不是『限制 worker 只能读』。不得把它描述成安全边界。」\n    ⚠️ 我第一版 grep 模式没匹配上、误报「未写明」——**本段第三次是我的检查误报，不是交付问题**。\n\n【明确未被证明的部分 —— 不随本次放行一并声称】\n  · **本包未做真机 `--run`**（spec §4 非目标）⇒ **完整生产链路从未真跑过**。\n  · 变异只覆盖 G4/G5/G7 ⇒ F2/F3/F4/F7/F8/F10 的功率不由本 gate 证明。\n  · ⛔ **`--add-dir` 不是安全边界**（实测 Bash 不受目录限制，worker 曾在工作区外成功执行\n    `sha256sum <绝对路径>`）。用户已拍板「接受可读全盘，安全性移到凭证下发管控」，\n    而**凭证下发的管控方案尚未实现** ⇒ ⛔ **带真凭证的 role（`code-remote`/`feishu`）\n    不得投入自动化研究**；V1 只跑无凭证的 `code-local`。\n"}
