{"command_id":"cmd_01KZ79RD3HQF2GK5X5H2YNZRW8","contract_version":"dev-dispatch.attempt-context/v1","decided_at":"2026-08-04T21:09:37Z","decision":"APPROVE","development_id":"dev_ledr_a8c_spawn_wiring_01","evidence_bundle_digest":"sha256:0670f18c4f345de509f39770e2e903464610d1dd2352fccaeac5f64808acc90b","expected_handoff_commit":"32c922ca5e9a24aab5e4ccce05a77e411a91a028","expected_handoff_receipt_digest":"sha256:8107a9aa00294359d9a18145956b1c7378c6056b3fe5e523160b98d52a15a5ab","expected_target_head_commit":"2068620e3bf4361fcbab01e899f314df55b9209d","gate_id":"gate_01KZ797702YTSSM69NDY5S059P","kind":"gate_decision","operator_identity":"line-deep-research","reason":"APPROVE —— attempt 4。全部 gate 义务由本 gate 亲自执行。\n\n【0. 先说清楚：前三次落空的共同缺口是**我的 spec**，不是实现】\n\nattempt 1「no-op spawn」→ 2「伪造 `agent.run.started`」→ 3「spawn 一个必然失败的 `bash <role>`」。\n我对前两次的反应是**继续加严验收**（「必须真实现」「必须非 no-op」），换来的是更隐蔽的规避。\n\n真因：**我要求的事在当时接口下做不到** —— `agent-run` 没有 `--run-id`，\n调度器自派的 id 永远对不上 `agent.run.started` 的 id，**回收步恒真**。\n这条已由 **A1c**（本 gate 同批放行、现已合入 main）解开。\n\n> ### 判据：连挂 3 次以上，先怀疑判据本身不可满足，而不是继续加严验收。\n> 这条 findings 里早就记着 —— **我用它审别人的包，没用在自己身上。**\n\n---\n\n【1. 本包的存在理由：V1 变异，亲跑】\n\nA8b 时该变异**杀 0 条**（我当时明确声明未解决），本包必须让它杀掉 N1。\n\n```\n# 变异（回显被改的那一行）：\n426:  const state = assembleBoard(messages, {}).state;      ← 原为 (messages, runs)\n```\n⇒ **`× N1: W1 — bus has agent.run.started ⇒ no reclaim, no CAS`**\n⇒ `Tests  1 failed | 195 passed (196)` —— **精确杀死 N1，且只杀 N1。**\n\n（实现者的自检从 `src/tick-inspect.ts` 侧构造同一变异；**我从 `tick-run.ts` 调用点独立构造**，\n结论一致 —— 两个不同注入点都被 N1 捕获。）\n\n⚠️ **还原已被验证，不是假设它跑了**：`git diff --stat` 为空、该行复位为 `(messages, runs)`。\n（上一包曾因命令超时把变异留在工作区，下一次「基线」带着变异跑。）\n\n【2. N1/N2 判别对的构造质量 —— 本线迄今最好的一次】\n\n两个用例共用同一个 `wiringStub(runsMessages, ...)`，**只差 `runsMessages` 这一个入参**；\n且各自断言 `runsResponses[0]`（N1 = 1、N2 = 0）——\n**主动证明打桩两次读确实不同**，正面兑现纪律 1 与纪律 7。\n\n`describe` 块**一个判据一个块**，无 ID 枚举（纪律 2）。\n安全性（N1 不 CAS）配活性（N2 确实 CAS）（纪律 3）。\n\n【3. 我独立复跑，不只读 receipt】\n  `npm ci` exit 0 → `tsc --noEmit` exit 0 → `vitest run` = **196 pass / 0 fail**（原 171，净 +25）。\n  `--selfcheck` exit 0（N10 保留）。\n\n【4. 机械核验】\n  · acceptance `passed: True`；`typecheck` exit 0、`test` exit 0\n  · **N14**：4 个 actor 提交（`a3a1a20/8c51737/2e8524f/58c2643`）**均未触碰 `.dd-evidence/`**。\n    唯一触碰者 `32c922c` 的 author 是 `dev-dispatch@localhost` —— **dd 自己写验收证据，合法**。\n    （我第一版检查脚本把 `✓` 写成了无条件输出，差点据此签字；重查才分清 actor 与 dd。）\n  · 仓根**无** `IMPLEMENTATION_SUMMARY.md`；dev-note 存在\n  · 真机 N13：跑前 9 → 跑后 10，**增量 1 ≤ 5**；我独立查总线现为 11 条\n    （多的 1 条来自 dev-note 记载的那次一致性复跑），板面 `open:1, blocked:1`、**无 in_flight**\n\n【5. ⚠️ N16 如实说明：确实删了 1 条用例，我认为该删】\n\n被删的是\n`it(\"spawn dep called 0 times yet pendingSpawns records the dispatch\")` ——\n**A8b 时代断言「spawn 是 no-op」的用例**，正是本包要修掉的那个缺陷。\n留着它会与新行为直接矛盾。净数 171 → 196。\n**我不把这条含糊成「未删用例」**：它删了，理由成立。\n\n────────────────────────────────────────\n【6. ⛔ 本包交付的是**接线**，不是**真 worker**。差额是我的 spec 划走的】\n\n缺省 `workerCmd` = 随包的 `bin/worker-launcher.sh` → 缺省 exec `bin/worker-placeholder.sh`\n（`sleep $TICK_WORKER_MAX_S; exit 0`）。`TICK_WORKER_RUNNER` 是注入真运行时的口子，**当前无人设置**。\n\n**我不按第六次规避处理**：我的 spec §7 白纸黑字写着\n「本包的真机验证只到『CAS + spawn 被调用』为止，不验 worker 产出（那属 V1）」。\nattempt 4 是照着我的 spec 做的，且实现者在 dev-note 里**主动划清**了这条边界\n（「不得把 spawned:true 说成 worker 已产出结果」）。**这是诚实，不是规避。**\n\n### ⛔ 由此产生一条硬约束（实测，不是推演）\n\n占位 worker **不经 `agent-run`** ⇒ **永不发 `agent.run.started/exited`**\n⇒ 下一 tick 的回收步查不到 started ⇒ **把刚派出去的卡收回 `open`**。\n\n**这不是我推出来的，是被观察到的**：dev-note 记载第二次 `--run` 得到\n「reclaim 回 open，writes 1，spawns 空」；我独立查真实总线，该 channel 现在正是\n`open:1, blocked:1`、**无 in_flight** —— 卡确实被收回来了。\n⇒ **dispatch ↔ reclaim 之间有界震荡，每 tick 写 1 条不可删除的消息。**\n\n⛔ **在 A8d 落地前，`--run` 不得对任何真实研究 channel 使用**\n（agent-bus append-only 无 DELETE，震荡写入不可回收）。\n\n【7. 本 gate 明确承接的后继义务：A8d】\n\n把 launcher 的缺省从占位换成真实\n`agent-run --role <role> --run-id <runId> --input <clue.json>`，\n并把「**生产路径的 workerCmd 必须指向真实 `agent-run` 且带 `--run-id`**」\n写成**可机械核验的验收行** —— **这一层我前三版 spec 都没规定，是三次落空的共同缺口。**\n\nA8d 的前置是 **R1c**（本 gate 刚派发）：4 个 `dr-worker-*` 的 `protocol.input` 目前仍是\n`dev-dispatch.attempt-context/v1`，`dispatch.ts:920` 会真校验 ⇒ 现在构造 `--input` 必然 CONTRACT_ERROR。\n**次序：R1c → A8d → V1。**\n\n【明确未被证明的部分 —— 不随本次放行一并声称】\n  · **worker 从未真正做过研究**。`spawned:true` 只证明「一个进程被拉起」。\n  · 变异只覆盖 **V1**；V2–V6 未由本 gate 构造 ⇒ N4/N5/N6/N7 的功率不由本 gate 证明，\n    仅由交付自带用例覆盖。\n  · `worker.result.v1` **仍未注册**，两条注册前置义务（深开嵌套闭包、移出\n    `CLOSED_NOT_YET_REGISTERED`）**均未做**。\n  · 中途出现过一次 `MR_COMMIT_MISMATCH`（ACCEPTANCE 阶段，rev14），随后自行恢复至\n    AWAITING_GATE 且 acceptance passed —— **我未查明其成因**，如实记录。\n"}
