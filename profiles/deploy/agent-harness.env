# D1 —— 「agent harness」研究部署配置（受版本管理；进 git / 可 diff / 可 review）。
# 加载：bin/deep-research-loop.sh --profile agent-harness（或 DEPLOY_PROFILE=agent-harness）。
# 加载顺序：显式 env > 本 profile > 内置缺省。
#
# ⛔ 以下 bus channel 由派发方于 2026-08-09 07:51Z 在生产 bus 上显式创建并复核（GET head_seq=0 msgs=0）。
#    bus 是 append-only 无 DELETE，写错不可回退。换研究时须重新创建一对 channel 并经 GET 复核，
#    不得复用旧 channel 名（一次核验、一次研究，不是永真）。
#    TICK_CHANNEL / EVIDENCE_CHANNEL 不再由内置缺省兜底——生产不显式提供即响亮失败（spec §1.2 / §2 E1 / E5）。

TICK_CHANNEL=research:agent-harness.index
EVIDENCE_CHANNEL=research:agent-harness.evidence
ALLOWED_ROOT=/data/code/self/agent-runtime
MAX_WRITES=96
# G4a —— 研究主问题（--question）：生产不传 ⇒ 收集段首个 triage 决策响亮失败。只加该键，无缺省。
RESEARCH_QUESTION=agent harness
# G4c —— doc channel（research.doc.v2 发布 channel）：派发方 2026-08-09 18:31Z 创建并复核（head_seq=0），
#   无缺省——未配置时 writeDoc 抛 MissingDocChannelError。
DOC_CHANNEL=research:agent-harness.docs
# G4d —— anchor-check 校验器二进制（绝对路径）：派发方 2026-08-09 实测该文件存在、240 行、可执行位已置。
#   未配置时 spawnAnchorCheck 抛错点名 ANCHOR_CHECK_BIN。
ANCHOR_CHECK_BIN=/data/code/self/katana/plugins/deep-research/skills/deep-research/loop-orchestration/tools/anchor-check.py
# 导出落点根（spec §1.3 / E6）：vault 根，导出件落 <EXPORT_ROOT>/DeepThought/<topic-slug>/。
# 注意：EXPORT_ROOT 必须是 vault 根（不含 DeepThought 段）；deriveExportPath 会追加 'DeepThought/'
#   （src/export.ts:56），此处若写成 …/DeepThought 会双重嵌套（2026-08-09 拍板：落 DeepThought/<主题>/）。
EXPORT_ROOT=/data/vault