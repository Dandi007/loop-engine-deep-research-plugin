# D1 —— 生产部署配置（受版本管理；进 git / 可 diff / 可 review）。
# 加载：bin/deep-research-loop.sh --profile production（或 DEPLOY_PROFILE=production）。
# 加载顺序：显式 env > 本 profile > 内置缺省。
#
# ⛔ 以下 bus channel 必须是**已核实存在**的真实 channel（bus append-only 无 DELETE，
#    写错不可回退）。TICK_CHANNEL / EVIDENCE_CHANNEL 不再由内置缺省兜底——
#    生产不显式提供即响亮失败（spec §1.2 / §2 E1 / E5）。

TICK_CHANNEL=research:v1-deep-research.index
EVIDENCE_CHANNEL=research:v1-deep-research.evidence
ALLOWED_ROOT=/data/code/self/agent-runtime
MAX_WRITES=96
# 导出落点根（spec §1.3 / E6）：vault 根，导出件落 <EXPORT_ROOT>/DeepThought/<topic-slug>/。
# 注意：EXPORT_ROOT 必须是 vault 根（不含 DeepThought 段）；deriveExportPath 会追加 'DeepThought/'
#   （src/export.ts:56），此处若写成 …/DeepThought 会双重嵌套（2026-08-09 拍板：落 DeepThought/<主题>/）。
EXPORT_ROOT=/data/vault
