# anchor-check fixtures directory

This directory contains test fixtures for anchor-check.py selftests.
The selftest creates its own test data at runtime, so this directory
serves as a placeholder for future fixture-based tests.