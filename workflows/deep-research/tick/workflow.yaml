# A10b —— 收敛只由板面状态决定；max_nodes 是**非绑定**的失控预算护栏，不是收敛机制（§1.1）。
# 根因（§0.1）：loop-engine 在主循环每圈顶部查 `done.size >= max_nodes ⇒ finish("max_nodes")`，
#   而 fleet 的 claim.complete 把非 {halt,drained} 终局当失败路由回 failure_status:open ⇒
#   seed 每轮被认领又退回 open ⇒ 永不「已排空」。上一版 max_nodes:1（后 2）会让「撞上 max_nodes」
#   成为收敛触发器：板面一旦需 ≥2 个 tick pass（done.size>=max_nodes）就重演该失败循环。
#   修复：把 max_nodes 设成明显非绑定的大值（本板面自然 drain 的 pass 数远小于它），
#   使收敛只由板面状态（tick 依 hasPendingWork 停止续投）确定性推出；max_rounds=16 仍是失控兜底。
#   （B1-guard 收敛成因测试钉死本配置，调回 1/2 即被拦截。）
#
# E0c10 D1（GT-A/GT-B）—— node_timeout / wall_clock 抬到 1810：
#   真机取证（GT-A，逐字照抄派发方）：tick 会被引擎级 `node_timeout` 杀掉，且**与板面规模无关**。
#     同一交付背靠背两跑：一次零超时、一次两次超时 ⇒ 超时是间歇性的。
#     `journal: {"identity":"tick","result":"[外部调用失败 status=TIMEOUT]\n","error":"exec"}`，
#     时长 39.9s / 30.5s；另一次单次 **904.2 秒**死于 TIMEOUT。⇒ 30s 对一个合法地要做
#     bus I/O + spawn + 等结果的 tick 就是太紧；**实测最大单 tick 耗时 = 904.2 秒**。
#   1810 的两条理由（GT-A/GT-B，⛔ 不得断言与某个源码常量对齐，spec §2 判据 3）：
#     (a) 声明的余量倍数 = 2.0×：1810 ≥ 904.2 × 2.0 = 1808.4（GT-A 的余量倍数，真机取证来源）；
#     (b) 1810 > DEFAULT_AGENT_RESULT_TIMEOUT_MS(900_000ms = 900s)（GT-B：闸刀必须大于
#         一次合法等待的预算，否则 readResult/readBody 的一次合法等待会被腰斩）。
#   wall_clock 与 node_timeout 同单位（秒），同 map：一个合法 tick 可达 904.2s，
#   wall_clock=60 会在 node_timeout 触发前先把整条 pipeline 节点杀掉（GT-A 同源），
#   故一并抬到 1810（同一余量倍数，同一 GT-A 取证）。max_nodes/max_retries/concurrency 不变。
#   （D1 判别性测试钉死 node_timeout >= 904.2 × 2.0；调回 < 904.2 即被拦截。）
limits: { max_nodes: 64, wall_clock: 1810, node_timeout: 1810, max_retries: 0, concurrency: 1 }
harness:
  sh: { runtime: bash, write: false }
seed:
  - identity: tick
    mode: fresh
    template: tick
    harness: sh
    payload:
      tick_entry: "{{tick_entry}}"
      tick_channel: "{{tick_channel}}"
      evidence_channel: "{{evidence_channel?}}"
      allowed_root: "{{allowed_root?}}"
      # E1b D1/D2/D7 —— content worker 的 spool 根目录（--content-spool-root）：由 fleet input 注入 tick.md，
      # 再传给 tick-entry --run。可选（`?` 后缀，与 allowed_root 同款）：缺省（null/空）⇒ tick.md 不传
      # --content-spool-root ⇒ tick-entry --run 用 DEFAULT_CONTENT_SPOOL_ROOT（tmpdir 下兜底）。
      content_spool_root: "{{content_spool_root?}}"
      # A10c —— 写入预算（--max-writes）：由 fleet input 注入 tick.md，再传给 tick-entry --run。
      max_writes: "{{max_writes}}"
      # E0c10 D5 —— 板面 clue 上限（--max-clues）：由 fleet input 注入 tick.md，再传给 tick-entry --run。
      # 可选（`?` 后缀，与 evidence_channel/allowed_root 同款）：缺省（null/空）⇒ tick.md 不传
      # --max-clues ⇒ tick-entry --run 用 DEFAULT_TICK_CONFIG.maxClues=64。
      max_clues: "{{max_clues?}}"
      # G4a —— 研究主问题（--question）：由 fleet input 注入 tick.md，再传给 tick-entry --run。
      research_question: "{{research_question}}"
      # G4c —— 研究 origin（--origin）：由 fleet input 注入 tick.md，再传给 tick-entry --run。
      research_origin: "{{research_origin?}}"
      # G4c —— doc channel（--doc-channel）：由 fleet input 注入 tick.md，再传给 tick-entry --run。
      doc_channel: "{{doc_channel?}}"
      # E0c3b §1.1 —— triage 触发阈值（--triage-threshold）：由 fleet input 注入 tick.md，
      # 再传给 tick-entry --run。与 MAX_WRITES 同款装配链（缺省 3）。
      triage_threshold: "{{triage_threshold}}"
      trigger_store_dir: "{{trigger_store_dir}}"
      loop_store_cli: "{{loop_store_cli}}"
      loop_engine_runner: "{{loop_engine_runner}}"
      # G4b —— 续投 trigger 的 body（claim.bind 已把 trigger body 绑进 pipeline input）。
      # 首个 seed 触发的 body 形如 {"seed":true}（无计数字段）；tick.md 判别后对首轮不传 --prev-*。
      # 续投 body 形如 {"tick":true,"coverage":<n>,"zeroGrowthRounds":<m>}；tick.md 解析后传 --prev-*。
      trigger_body: "{{trigger_body}}"
